---
name: Bug report
about: Something the compiler, validator, server or studio gets wrong
labels: bug
---

**What happened, and what you expected instead**

**The document that triggers it**

<!-- The shot spec or model recipe, or the smallest cut of it that still
     fails. This is usually enough to reproduce on its own, which is most of
     the reason the architecture works this way. Do not attach a .blend. -->

```json

```

**Where it failed**

<!-- The entity id and the build stage from the error, if you got one.
     "Failed at resolve_assets, entity hero_cloak" beats a screenshot. -->

**Versions**

- Blender:
- OS:
- `tests/run_all.sh` passes on a clean checkout: yes / no
